import SigninForm from "@/features/auth/SigninForm";

export default function page() {
  return (
    <div className="min-h-screen flex items-center justify-center p-4 bg-stone">
      <SigninForm />
    </div>
  );
}
